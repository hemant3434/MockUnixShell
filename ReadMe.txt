Java coded Mock Unix Shell with a fully functional file system with commonly used Unix commands.

Commands implemented:
cd, mv, cp, get, pushd, popd, man, save, load, cat, mkdir, history, pwd, tree and find